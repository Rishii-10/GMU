"""
Synthetic/fabricated test fixtures (Phase 8) -- built where no real data
exists (facility master data, village coordinates, calibration samples,
follow-up conversation scripts). Deliberately balanced across classes
(disease, severity, outcome) rather than skewed to a few cases, per the
user's test-data requirement: exercise the system broadly and honestly,
not cherry-picked to make it look better than it is.
"""
